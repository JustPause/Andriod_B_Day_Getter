<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.BDayGetter" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryVariant">@color/primaryLight</item>
        <item name="colorOnPrimary">@color/textDark</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryLight</item>
        <item name="colorSecondaryVariant">@color/secondaryLight</item>
        <item name="colorOnSecondary">@color/textLight</item>
        <!-- Status bar color. -->
        <item name="colorPrimaryDark">@color/primaryLight</item>

        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textLight</item>
    </style>
</resources>